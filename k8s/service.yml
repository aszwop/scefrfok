apiVersion: v1
kind: Service
metadata:
  name: vsffrwrw
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vsffrwrw"
    port: 8080
  selector:
    app: vsffrwrw
